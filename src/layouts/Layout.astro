---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="🇹🇼🇹🇼🇹🇼⚾️⚾️⚾️🏆🏆🏆" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <meta property="og:site_name" content="填寫中華隊道歉表">
    <meta property="og:title" content="填寫中華隊道歉表">
    <!-- <meta property="og:url" content="http://{{ site.url }}{{ site.baseurl }}{{ page.url }}"> -->
    <meta property="og:description" content="對不起中華隊，我一開始不看好你">
    <meta property="og:image" content="/sorry.jpg" id="og-image">

    <title>{title}</title>
  </head>
  <body>
    <div class="base">
      <main><slot/></main>
    </div>
  </body>
</html>
<style is:global>
  :root {
  }
  html {
    font-family: system-ui, sans-serif;
    background: #eeeeee;
    min-height: 100vh;
  }

  body {
  }

  .base {
    height: 100vh;
    display: grid;
    align-content: center;
    justify-content: center;
  }

  main {
    margin: auto;
    padding: 1rem;
    max-width: calc(100% - 2rem);
    aspect-ratio: 1 / 1.414;
    background-color: white;
    font-size: 20px;
    line-height: 1.6;
		box-shadow: 4px 4px 16px -3px rgba(0,0,0,0.1);
    /* border: 1px solid black; */
    height: 90vh;
  }
</style>
